pipeline {
  agent any
  stages {
    stage('checkout code') {
      steps {
        git(url: 'https://github.com/Naresh-varma/topTransactions', branch: 'main')
      }
    }

    stage('') {
      steps {
        sh 'ls'
        git(url: 'https://github.com/Naresh-varma/topTransactions', branch: 'main')
      }
    }

  }
}